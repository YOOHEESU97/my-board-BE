spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board_db?serverTimezone=Asia/Seoul
    username: root         # 네 MySQL 사용자명
    password: "1q2w3e4r!!"      # 네 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update     # 개발 중엔 update, 나중엔 validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 서버 포트 (기본은 8080)
  server:
    port: 8080
